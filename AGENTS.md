# AI Agent Rules for KindField

## Session Init Protocol

Before reading code or making changes, run:
```bash
cat ~/.kindpath/HANDOVER.md
python3 ~/.kindpath/kp_memory.py dump --domain gotcha
python3 ~/.kindpath/kp_memory.py dump
```

---

## What This Is

KindField is the shared epistemological foundation — a Python package providing
KindEarth metric field definitions consumed by KindSense and other KindPath tools.

## Rules

- Field definitions are the source of truth for how relational signals are modelled
- Any changes to field schemas must be backward compatible
- Read `KINDFIELD.md` before modifying field structures
- Metrics serve insight, not surveillance — sovereignty is non-negotiable
- Follow KindPath doctrine: benevolence, syntropy, sovereignty

## Operational Commands

- **Install**: `pip install -e .`

## Security Mandates

- No personally identifiable data in source
- Metric fields are anonymised signals only
